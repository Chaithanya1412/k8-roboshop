 apiVersion: app/v1
 kind: Deployment
 matadata:
  name: mongodb
  namespace: roboshop
  #there are replica set labels
  labels:
    project: roboshop
    component: mongodb
    tier: db
 spec:
  replicas: 1
  selector:
  # these labels are used for pod
  matchlabels: 
    project: roboshop
    component: mongodb
    tier: db
  # pod defination
  template:
    metadata:
      labels:
        project: roboshop
        component: mongodb
        tier: db
    spec:
      containers:
      - name: mongodb
        image: joindevops/mongodb:v1